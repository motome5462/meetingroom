<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- ✅ Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" />

  <!-- ✅ Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" />

  <title>index</title>
</head>

<!-- <body style="background: linear-gradient(90deg, rgb(210, 243, 157), rgb(207, 166, 245), rgb(150, 241, 229), rgb(255, 155, 155), rgb(136, 136, 255), rgb(253, 255, 158), rgb(255, 253, 155));"> -->
<body style="background: linear-gradient(90deg, #dfefffce);">
<!-- <body style="background: linear-gradient(90deg, #dfefff, #ffeaea);">
<body style="background: linear-gradient(135deg, #cce7ff, #ffd6d6);">
<body style="background: linear-gradient(120deg, #e6f0ff, #ffe6e6);"> -->

  <header>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Meeting Room</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <% if (user) { %>
          <li class="nav-item">
            <a class="nav-link" href="/insert">จองห้องประชุม</a>
          </li>
          
          <% if (user.role === 'admin') { %>
            <li class="nav-item">
              <a class="nav-link" href="/admin/admindashboard">ข้อมูลการจอง</a>
            </li>
          <% } else { %>
            <li class="nav-item">
              <a class="nav-link" href="/user/dashboard">ข้อมูลการจอง</a>
            </li>
          <% } %>
          <% } %>
        <li class="nav-item">
          <a class="nav-link" href="/schedule/monthly">ตารางรายเดือน</a>
        </li>
        <!-- <li class="nav-item">
          <a class="nav-link" href="/schedule/daily">ตารางรายวัน</a>
        </li> -->
        <% if (user) { %>
          <li class="nav-item">
            <a class="nav-link" href="/auth/logout">Logout</a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link" href="/auth/login">Login</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
  </header>


  <!-- ✅ Bootstrap Bundle (JS + Popper) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
