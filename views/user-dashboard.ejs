<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <title>User Dashboard</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="bg-light">
  <header class="mb-4">
    <%- include('index.ejs') %>
  </header>

  <div class="container mt-5">
    <div class="card shadow p-4">
      <h2 class="text-center mb-3">User Dashboard</h2>

      <div class="mb-4 text-center">
        <span class="fw-bold">Welcome, <%= employeeName %>!</span>
      </div>

      <div class="card border-primary shadow-sm">
        <div class="card-header bg-primary text-white fw-bold fs-5">
          Your Meeting Bookings (<%= meetings.length %>)
        </div>
        <div class="card-body">
          <% if (meetings.length === 0) { %>
            <div class="alert alert-info text-center">You have no meeting bookings.</div>
          <% } else { %>
            <% meetings.forEach(room => { %>
              <div class="card mb-3 shadow-sm border-primary">
                <div class="card-body">
                  <h5 class="card-title text-success"><%= room.room %></h5>
                  <p class="card-text mb-1"><strong>วัตถุประสงค์:</strong> <%= room.purpose || '-' %></p>
                  <p class="card-text mb-1">
                    <strong>ผู้เข้าร่วม:</strong>
                    <%= room.participants?.length || 0 %> คน
                    <% if (room.participants && room.participants.length > 0) { %>
                      (<%= room.participants.map(p => p.name.split(' ')[0]).join(', ') %>)
                    <% } %>
                  </p>
                  <p class="card-text mb-1">
                    <strong>วันที่:</strong> <%= new Date(room.datetimein).toLocaleDateString() %>
                    <strong>เวลา:</strong>
                    <%= new Date(room.datetimein).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' }) %> -
                    <%= new Date(room.datetimeout).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' }) %>
                  </p>
                  <p class="card-text mb-1"><strong>สถานะ:</strong> <span class="badge bg-<%= room.approval === 'อนุมัติ' ? 'success' : 'warning' %>"><%= room.approval %></span></p>
                  <a href="/admin/edit-meeting/<%= room._id %>" class="btn btn-warning btn-sm me-2">แก้ไข</a>
                  <button class="btn btn-danger btn-sm" onclick="deleteMeeting('<%= room._id %>')">ลบ</button>
                </div>
              </div>
            <% }); %>
          <% } %>
        </div>
      </div>
    </div>
  </div>

  <script src="/javascripts/userdashboard.js"></script>
</body>
</html>