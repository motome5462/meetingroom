<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Meeting Room Schedule</title>
  <link rel="stylesheet" href="/stylesheets/schedule.css">
  <link href="https://fonts.googleapis.com/css2?family=Prompt&display=swap" rel="stylesheet">
</head>
<body style="background: rgb(216, 255, 152);">
  <header class="mb-4">
    <% include index.ejs %>
  </header>

  <div class="header-container">
    <h2>ตารางการประชุม</h2>
    <input type="date" id="datePicker" value="<%= selectedDate %>" />
  </div>

  <div class="schedule-wrapper">
    <div class="schedule-container">
      <!-- Left column -->
      <div class="left-column">
        <div class="blank-cell">ห้องประชุม</div>
        <div class="rooms" id="roomsCol"></div>
      </div>

      <!-- Timeline -->
      <div class="timeline" id="timeline"></div>
    </div>
  </div>

  <script>
    // Make selectedDate available to frontend JS
    const selectedDateFromServer = "<%= selectedDate %>";
  </script>

  <script src="/socket.io/socket.io.js"></script>
  <script src="/javascripts/schedule.js"></script>

    <div id="meetingModal" class="modal">
    <div class="modal-content">
      <span class="close-button">&times;</span>
      <h3>รายละเอียดการประชุม</h3>
      <p><strong>หัวข้อ:</strong> <span id="modalPurpose"></span></p>
      <p><strong>ผู้จอง:</strong> <span id="modalEmployee"></span></p>
      <p><strong>ผู้เข้าร่วม:</strong></p>
      <ul id="modalParticipants"></ul>
      <p><strong>เวลา:</strong> <span id="modalTime"></span></p>
    </div>
  </div>
</body>
</html>
