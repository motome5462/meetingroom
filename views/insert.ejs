<!DOCTYPE html>
<html>
<head>
  <title>Meeting Room Request</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
</head>
<body>
  <header class="no-print mb-4">
    <% include index.ejs %>
  </header>

<% const safeSuccess = typeof success !== 'undefined' ? success : false; %>
<div class="container mt-4">
  <% if (safeSuccess) { %>
    <div class="alert alert-success">✅ ส่งคำขอสำเร็จแล้ว</div>
  <% } %>

<form action="/insert" method="POST">
  <!-- Employee ID input -->
  <div class="mb-3">
    <label>รหัสพนักงานผู้ขอใช้ห้องประชุม (Employee ID)</label>
    <input type="number" class="form-control" name="employeeid" required>
  </div>

  <!-- Date and Time Inputs -->
  <div class="mb-3">
    <label>วันที่ต้องการใช้</label>
    <input type="date" class="form-control" name="date" required>
  </div>

  <div class="row mb-3">
    <div class="col">
      <label>เวลาตั้งแต่</label>
      <input type="time" class="form-control" name="timein" required>
    </div>
    <div class="col">
      <label>ถึง</label>
      <input type="time" class="form-control" name="timeout" required>
    </div>
  </div>

  <!-- Room Selection -->
  <div class="mb-3">
    <label>เลือกห้อง</label><br/>
    <div class="form-check form-check-inline">
      <input class="form-check-input" type="radio" name="room" value="Room A" required>
      <label class="form-check-label">ห้องประชุม 1</label>
    </div>
    <div class="form-check form-check-inline">
      <input class="form-check-input" type="radio" name="room" value="Room B">
      <label class="form-check-label">ห้องประชุม 2</label>
    </div>
    <div class="form-check form-check-inline">
      <input class="form-check-input" type="radio" name="room" value="Room C">
      <label class="form-check-label">ห้องประชุม 3</label>
    </div>
  </div>

  <!-- Participants -->
  <div class="mb-3">
    <label>รหัสพนักงานผู้เข้าร่วม</label>
    <div id="participantFields">
      <input type="number" class="form-control mb-2" name="participants[]" placeholder="Employee ID">
    </div>
    <button type="button" class="btn btn-secondary btn-sm" onclick="addParticipant()">➕ เพิ่มผู้เข้าร่วม</button>
  </div>

  <!-- Purpose -->
<div class="mb-3">
  <label>วัตถุประสงค์</label>
  <select class="form-select" name="purpose" id="purposeSelect" required>
    <option value="ประชุมภายในองค์กร">ประชุมภายในองค์กร</option>
    <option value="ประชุมระหว่างองค์กร กับ หน่วยงาน">ประชุมระหว่างองค์กร กับ หน่วยงาน</option>
    <option value="อบรม / สัมมนา">อบรม / สัมมนา</option>
    <option value="อื่น ๆ">อื่น ๆ</option>
  </select>

  <input type="text" class="form-control mt-2" name="customPurpose" id="customPurposeInput" placeholder="ระบุวัตถุประสงค์อื่น ๆ" style="display:none;">
</div>

  <!-- Equipment -->
  <div class="mb-3">
    <label>อุปกรณ์ที่ต้องการใช้</label>
    <input type="text" class="form-control" name="equipment" placeholder="เช่น โปรเจคเตอร์, ไมค์ ฯลฯ">
  </div>

  <!-- Remark -->
  <div class="mb-3">
    <label>หมายเหตุเพิ่มเติม</label>
    <textarea class="form-control" name="remark" rows="3"></textarea>
  </div>

  <button type="submit" class="btn btn-primary">ส่งคำขอใช้ห้องประชุม</button>
</form>
</div>

<script src="/javascripts/insert.js"></script>
</body>
</html>
